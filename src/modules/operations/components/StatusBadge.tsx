export function StatusBadge({ status }: { status?: string }) { const normalized = status ?? 'active'; const tone = normalized === 'active' ? 'text-emerald-300 bg-emerald-400/10' : normalized === 'archived' ? 'text-slate-400 bg-slate-400/10' : 'text-amber-300 bg-amber-400/10'; const label = normalized === 'active' ? 'Ativo' : normalized === 'archived' ? 'Arquivado' : 'Inativo'; return <span className={`rounded px-1.5 py-0.5 text-[10px] ${tone}`}>{label}</span> }

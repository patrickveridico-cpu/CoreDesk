export function ConfirmDialog({ open, title, message, confirmLabel = 'Confirmar', danger = false, onConfirm, onCancel }: { open: boolean; title: string; message: string; confirmLabel?: string; danger?: boolean; onConfirm: () => void; onCancel: () => void }) { if (!open) return null; return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><div className="w-full max-w-sm rounded-lg border border-core-line bg-core-raised p-4 shadow-2xl"><h2 className="text-sm font-semibold text-white">{title}</h2><p className="mt-2 text-xs text-slate-400">{message}</p><div className="mt-4 flex justify-end gap-2"><button onClick={onCancel} className="rounded border border-core-line px-3 py-2 text-xs outline-none focus-visible:ring-2 focus-visible:ring-core-accent/50">Cancelar</button><button autoFocus onClick={onConfirm} className={`rounded px-3 py-2 text-xs font-semibold outline-none focus-visible:ring-2 focus-visible:ring-core-accent/50 ${danger ? 'bg-red-500/90 text-white hover:bg-red-500' : 'bg-core-accent text-slate-950'}`}>{confirmLabel}</button></div></div></div> }

import type { OperationsData } from './models'
export const emptyOperationsData = (): OperationsData => ({ schemaVersion: 3, operationalCompanies: [], bases: [], insurers: [], specialties: [], pricingTables: [], quotes: [], legacyHistory: [], audit: [], idMap: {} })
export function validateOperationsData(value: unknown): OperationsData {
  const source = value && typeof value === 'object' ? value as Partial<OperationsData> : {}
  const withStatus = <T extends { status?: string }>(items: T[]) => items.map((item) => ({ ...item, status: item.status === 'inactive' || item.status === 'archived' ? item.status : 'active' }))
  return { ...emptyOperationsData(), ...source, schemaVersion: 3, operationalCompanies: Array.isArray(source.operationalCompanies) ? source.operationalCompanies : [], bases: Array.isArray(source.bases) ? withStatus(source.bases) : [], insurers: Array.isArray(source.insurers) ? withStatus(source.insurers) : [], specialties: Array.isArray(source.specialties) ? withStatus(source.specialties) : [], pricingTables: Array.isArray(source.pricingTables) ? withStatus(source.pricingTables) : [], quotes: Array.isArray(source.quotes) ? source.quotes : [], legacyHistory: Array.isArray(source.legacyHistory) ? source.legacyHistory : [], audit: Array.isArray(source.audit) ? source.audit : [], idMap: source.idMap && typeof source.idMap === 'object' ? source.idMap : {} } as OperationsData
}

# Backups e restauração

Backups de Operações ficam em `operations/*.backup.json` e não incluem dados de sessão do WhatsApp. A criação e restauração são operações explícitas, validadas no processo main e registradas na auditoria.
